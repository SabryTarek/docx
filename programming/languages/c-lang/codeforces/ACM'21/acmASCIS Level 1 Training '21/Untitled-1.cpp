#include <iostream>

int main()
{
	unsigned short int a,
		               b,
		               c,
		               ans;
    while(std::cin >> a >> b >> c)
`   {
        ans = (2a + 3b) × 5c
        std::cout.precision(6);
        std::cout << ans <<"\n\n\n";
    }
    retun 0;
}